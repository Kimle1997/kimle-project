---
const {   heading_title, button } = Astro.props;
---

<section class="cta-banner-section section">

  <div class="container">

    <div class="row">

      <div class="heading col-md-12">
        <span class="sub-title">
          {heading_title?.sub_title}
        </span>

        <h2 class="heading-title">
          {heading_title?.title}
        </h2>

        <p class="description">
          {heading_title?.description}
        </p>
        {
        button?.map((item) => (
          <a
            href={item.btn_link}
            class={`btn btn-${item.variant}`}
            target={item.target ? "_blank" : "_self"}
          >
            {item.btn_text}
          </a>
        ))
      }
      </div>

    </div>

  </div>

</section>
<style>
    .cta-banner-section {
        background-color: #161616;
    }
    .heading {
        text-align: center;
    }
    p {
        margin-bottom: 40px;
        max-width: 50%;
        margin-left: auto;
        margin-right: auto;
    }
    p,
    .heading-title {
        color: #fff;
    }
    .btn-outline {
          font-size: 12px;
    line-height: 16px;
    letter-spacing: 0.1em;
    font-weight: 600;
    font-family: "Hanken Grotesk", sans-serif;
    font-style: normal;
    margin-left: 20px;
    }
    @media(max-width: 600px) {
  p {
    max-width: 100%;
  }
}
</style>